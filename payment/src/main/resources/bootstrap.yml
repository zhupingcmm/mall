spring:
  application:
    name: mall-payment
  cloud:
    nacos:
      config:
        server-addr: 127.0.0.1:8848
        file-extension: yml
  profiles:
    include: qa
#  config:
#    import:
#      - optional:nacos:mall-payment.yml